include "application.base.conf"

spray.can {
  verbose-error-messages = on
}

bitcoin {
  network = "org.bitcoin.production"
  wallet {
    directory = "/home/ubuntu/bitcoinProduction"
    prefix = "wallet-org.bitcoin.prod"
  }
}

notaryService {
  interface = "0.0.0.0"
  port = 8080
}
